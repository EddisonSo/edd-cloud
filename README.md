# edd-cloud

Monorepo root for edd-cloud projects.

## Repos
- edd-cloud-interface: frontend and backend for the web UI (GFS namespace prefix defaults to `/sfs`, stored as `sfs/<namespace>/<file>`)
- go-gfs: Go-based storage service
- manifests: deployment and configuration manifests
